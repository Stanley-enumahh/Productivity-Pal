{
  "routes": [
    {
      "src": "/[^.]+\\.[a-z]+", // Match files with extensions (.js, .css, etc.)
      "headers": { "Cache-Control": "public, max-age=31536000" }
    },
    {
      "src": ".*", // Fallback to index.html for SPA routes (React Router)
      "dest": "/index.html"
    }
  ]
}
